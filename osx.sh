#!/bin/bash

# This installs the base packages I like having on every osx system I touch.


/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"
brew cask install vscodium
